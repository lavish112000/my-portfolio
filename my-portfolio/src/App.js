// Legacy placeholder: this nested app copy is not used by the active CRA build (root /src).
// Keeping this file minimal avoids duplicate static analysis noise.
const App = () => null;

export default App;

